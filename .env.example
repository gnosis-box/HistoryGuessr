# History Guessr — Circles Group (when deployed)
# VITE_HIST_GROUP_ADDRESS=0xYourHistoryGuessrGroupAddress

# Optional: public URL override (Vercel custom domain; default = current origin)
# VITE_APP_URL=https://history-guessr.vercel.app

# Garage / Vercel demo — skip trust gate (do not use in production)
# VITE_DEV_RELAX_TRUST=true
